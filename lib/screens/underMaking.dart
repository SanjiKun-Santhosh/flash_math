import 'package:flash_math/screens/template.dart';
import 'package:flutter/material.dart';

class UnderMaking extends StatelessWidget {
  const UnderMaking({super.key});

  @override
  Widget build(BuildContext context) {
    return Template(child: Text("Under progress",style: TextStyle(fontSize: 30),));
  }
}
